[pytest]
markers =
    safe: Tests using temporary databases and mocked PLC communication
    integration: Integration tests without production resources
    plc_live: Supervised tests that can communicate with a real PLC
    destructive: Tests that can modify external resources
    legacy: Legacy compatibility tests
